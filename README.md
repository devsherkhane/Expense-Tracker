#  Expense Tracker (Go + Vue + MySQL)

A full-stack Expense Tracker web application built using **Go (Gin Framework)** for backend, **Vue.js** for frontend, and **MySQL** for database.
This project helps users manage their daily expenses by adding, viewing, and deleting records with categories and dates.

------
